describe('update_subscription()', function () {
  it('should return error if `sub_details` is not object.');
  it('should return error if `email` is not valid email.');
  it('should return error if `active` is neither 0 nor 1.');
});
