describe('comment_like()', function () {
  it('should return error if `comment_id` is not string.');
  it('should update `comment_like_count` if comment is approved.')
  it('should return error if `comment_id` is not provided.');
  it('should succeed.');
});
