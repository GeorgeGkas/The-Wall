describe('delete_author()', function () {
  it('should return error if `author_email` is not provided or is false value.');
  it('should return error if `author_email` is not valid email.');
  it('should succeed.');
});
