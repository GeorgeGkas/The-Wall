describe('update_comment()', function () {
  it('should return error if `select_details` is not object.');
  it('should return error if `post_id` is not valid post.');
  it('should return error if `state` is not valid state.');
  it('should update `post_comment_count` if comment is approved.');
  it('should add more points to `post_feature_dynamic` if comment is approved.');
  it('should update.');
});
