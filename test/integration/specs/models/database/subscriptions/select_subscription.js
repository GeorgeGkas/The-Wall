describe('select_subscription()', function () {
  it('should return error if `sub_email` is not string.');
  it('should return all subscriptions if string is `*`.');
  it('should return error if `sub_email` is not valid email.');
});
